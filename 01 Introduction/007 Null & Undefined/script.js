// let age ;
let age = null; //it is effectively zero when operation is performed otherwise null

console.log(age, age + 3, `the age is ${age} years old`)


// when not declared it is undefined and when such variable is operated it returns NaN.