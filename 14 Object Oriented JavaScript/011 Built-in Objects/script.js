const array = new Array(1, 2, 3, 4, 5)
console.log(array)



//! here we are storing the all the methods in the ARRAY prototype and
// inheriting properties form the OBJECT prototype


const xmlHttp = new XMLHttpRequest()
console.log(xmlHttp)


//? it has its own methods and gets properties from "XMLHttpRequest" and then from the 'XMLHttpRequestEventTarget' and then from the 'EventTarget' and finally at last inherits from the OG Object

